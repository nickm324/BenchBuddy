# Display

Display owns screen rendering boundaries for BenchBuddy.

This area is intended for display driver integration, drawing surfaces, view composition helpers, and rendering lifecycle abstractions.

Feature modules should describe what they need to show, while this layer handles how it is presented on the device display.
