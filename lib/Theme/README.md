# Theme

Theme owns shared visual styling for BenchBuddy.

This area is intended for color palettes, typography scales, spacing values, icon references, and reusable visual tokens.

Display and modules should use theme definitions instead of hard-coded presentation values.
